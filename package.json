{
  "name": "battle-snakes",
  "version": "1.0.0",
  "description": "Multiplayer snake game with WebSocket support",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run clean && npm run build:shared && npm install && npm run start:apps",
    "start:apps": "npm run dev:server & npm run dev:client",
    "dev:client": "npm run dev -w @battle-snakes/client",
    "dev:server": "npm run dev -w @battle-snakes/server",
    "build:shared": "npm run clean:shared && npm run build -w @battle-snakes/shared",
    "clean:shared": "rm -rf packages/shared/dist",
    "clean": "rm -rf packages/*/dist node_modules packages/*/node_modules"
  },
  "keywords": [
    "snake",
    "websocket",
    "game",
    "multiplayer"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2"
  }
}
