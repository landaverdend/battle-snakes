{
  "name": "battle-snakes",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run nuke && npm run start",
    "nuke": "rm -rf node_modules packages/*/node_modules packages/*/dist && npm i",
    "start": "cd packages/shared && npm run build && cd ../.. && concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "npm run dev -w @battle-snakes/client",
    "dev:server": "npm run dev -w @battle-snakes/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
