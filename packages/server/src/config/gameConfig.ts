
export const TICK_RATE = 10; // 10 ticks per second.
export const TICK_RATE_MS = 1000 / TICK_RATE;

export const DEFAULT_GRID_SIZE = 40;
export const DEFAULT_FOOD_COUNT = 10; // There should always be at least one food on the board. Maybe make this configurable?
export const DEFAULT_GROWTH_RATE = 5;