import { Game } from '@battle-snakes/shared';

export class LocalGame extends Game {
  constructor(gridSize: number) {
    super(gridSize);
  }

  start() {}

  tick(deltaTime: number) {

  }

  stop() {
    
  }
}
